spring:
  application:
    name: center-common-user-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 # nacos地址
      config:
        file-extension: yml # 文件后缀名
        group: FOR_YOURSELF
      discovery:
        group: FOR_YOURSELF
